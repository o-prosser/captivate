import { Heading } from "@/ui/typography";

export const metadata = {
  title: "Maths",
};

const Maths = () => {
  return (
    <>
      <Heading>Maths</Heading>
    </>
  );
};

export default Maths;
